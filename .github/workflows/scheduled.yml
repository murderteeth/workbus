name: scheduled
on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: announce
        run: echo "workbus feasibility run"
      - name: environment
        run: node -v && uname -a
